#!/bin/sh
# This hook automatically sets the version number for the CMCF website
# place this in .git/hooks within the live repository (eg /var/website/cmcf-website/.git/hooks/)
# to automatically update the version file every time a pull is done

# The file should be renamed 'post-merge' and made executable

exec git describe  | sed 's/.0-/./g' > cmcf/VERSION
